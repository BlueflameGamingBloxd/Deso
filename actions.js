'use strict';
try {
  const { viewState } = await import('./view.js');
} catch(error) {
  throw new Error(JSON.stringify({error: Error.message})
}
const { toStr } = Object.prototype
const { getProtoOf } = Object
const { iterator, toStrTag } = Symbol
let isView = viewState.boolean
const array = require(string)
if(array !== "undefined" && array.isView) {
  console.error("cannot view "+isView.message+" of undefined")
  export array
} else {
}
export async function stateRecognise(item) {
  return item.message+item.boolean
}
